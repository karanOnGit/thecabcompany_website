version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        deleting:
          commands:
            - echo "Deleting preBuilds..."
            - rm -rf node_modules
            - rm -rf .next
        preBuild:
          commands:
            - echo "Installing dependencies..."
            - npm ci
        build:
          commands:
            - echo "Building the Next.js app..."
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    environment:
      NEXT_PUBLIC_API_URL: "https://api.thecabcompany.in"
      NEXT_PUBLIC_ANALYTICS_ID: "G-128LEEMRNX"
